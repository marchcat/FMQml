# Status - 2026-05-15

## Done today

- Fixed folder loading regressions and kept ordinary folders fast.
- Stabilized rename, create, delete, copy, and move so they update panels without forcing full-window refreshes in the common case.
- Fixed `Escape`, `Ctrl+A`, and the path editor focus flow.
- Reworked the loading/status rail so large-folder loads are visible and readable in split view.
- Refined list, grid, sidebar, and toolbar state colors for hover, current item, and selection.
- Tuned the dark theme away from the muddy/green cast and kept light mode clear.
- Fixed Windows file icons so executables do not all inherit the first cached icon.
- Added an application icon using the free Breeze file manager SVG from KDE (`system-file-manager.svg`).
- Implemented a modern Help window (F1) listing keyboard shortcuts and core features.
- Added a discoverable info button to the main toolbar to trigger the help window.

## Notes

- The app icon is intentionally taken from the existing free in-repo asset set.
- Large-folder loading still uses the bottom rail rather than blocking the UI.
